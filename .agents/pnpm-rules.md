# PNPM Execution Rules

Always use `pnpm` as the package manager for running tasks, commands, tests, or installing dependencies in this repository.
Never use `npm` or `yarn` commands.

Examples:
- Run tests: `pnpm test`
- Run dev server: `pnpm dev`
- Install dependencies: `pnpm install <package>`
