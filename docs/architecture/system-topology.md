# System Topology (Dimension 1)

> Generated by Graphify on June 8, 2026. Last updated: June 8, 2026.

## Diagram

```mermaid
graph TD
  subgraph Client_App_UI [Client App UI]
    HomeScreen[HomeScreen / Nav]
    GameRunner[GameRunner Outer Shell]
    GameRunnerInner[GameRunnerInner Core Screen]
    AddQuestionsWizard[Socratic AI Question Generator]
    CheatSheetTerminal[Cheat Sheet Terminal Drawer]
  end

  subgraph Client_State_Providers [Client State & Logic Providers]
    GameEngineProvider[GameEngineProvider React Context]
    AchievementProvider[AchievementProvider Root Context]
    StatsProvider[StatsProvider / Context]
    ActiveSubjectStore[ActiveSubjectStore]
  end

  subgraph Persistence_API [Persistence / Browser API]
    LocalStorage[(Browser LocalStorage)]
    DOMPurify[isomorphic-dompurify Sanitizer]
  end

  subgraph External_Systems [External Systems]
    LLM[External LLM: Gemini / Claude / GPT]
  end

  HomeScreen --> GameRunner
  GameRunner --> GameEngineProvider
  GameRunnerInner --> GameEngineProvider
  GameRunnerInner --> StatsProvider
  GameRunnerInner --> CheatSheetTerminal
  AddQuestionsWizard --> DOMPurify
  AddQuestionsWizard --> LLM
  GameEngineProvider --> LocalStorage
  AchievementProvider --> LocalStorage
  StatsProvider --> LocalStorage
```

## Analysis Notes

- **Critical paths:** The interaction between `GameRunnerInner` and `GameEngineProvider` is the main loop of the app. It reads and writes active quiz state.
- **Risk areas:** The `AddQuestionsWizard` directly compiles prompt outputs that are pasted back into the browser. If the input is not sanitized properly, it could lead to XSS. `isomorphic-dompurify` is used to mitigate this.
- **Stale/dead code suspects:** None. The architecture is lean and offline-first.
