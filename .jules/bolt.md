2024-07-02 - [O(N^2) Latency Fixes], Learning: In performance-critical loops like PDF text extraction or JSON stream repair, using string concatenation (`+=`) creates high latency and O(N^2) memory constraints because strings are immutable in JS, causing excessive allocation and garbage collection. Action: Swapped deep string concatenations to chunked Array.push() and .join("") mechanisms in `subject-importer-steps.tsx` and `subject-persistence.ts`. Replaced `Uint8Array.from` with standard native loops inside `subject-sharing.ts` to prevent O(N) allocation loops.
